/**
 * настройки шоурума вынесены в отдельный файл,
 * чтобы продемонстрировать работу метода defaults
 */
(function () {
    // сохраняем некоторые данные для шоурума
    // в этот момент объекта App.Showroom еще не существует
    // по этому данные не передаются ему на прямую, а лишь
    // остаются в хранилище
    App.defaults("App.Showroom", {
        flickrKey: "7838fb0b74fde1a326e8b1cdd6b1f2c5",
        tags: "cat,dog"
    });
}());
